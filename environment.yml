name: video_loop_finder
channels:
  - conda-forge
  - defaults
dependencies:
  - opencv
  - notebook
  - matplotlib
  - jupyter
  - docopt
  - schema
prefix: /hd_data/anaconda3/envs/video_loop_finder

